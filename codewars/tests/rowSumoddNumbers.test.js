const rowSumOddNumbers = require("../../codewars/kata/rowSumOddNumbers");

describe("rowSumOddNumbers", () => {
  describe("should check the input string", () => {
    test("should check the input argument", () => {
      expect(rowSumOddNumbers.length).toEqual(1);
    });
  });
});
